# V18.59 稳定整理版

本版本不改变私人导航主页的功能逻辑和视觉布局，重点处理项目结构与长期维护。

## 已完成
- 根目录、英文页、About 页统一进入 `/cn/index.html`，移除旧 WebStack 广告与第三方统计代码。
- 404 页面替换为私人导航自有页面。
- `CNAME` 修正为 `dumbo.ccwu.cc`。
- 新增 Cloudflare Pages `_headers`，增加基础安全响应头。
- 统一前端、配置导出、健康检查和 API 错误信息版本号为 18.59。
- 配置备份文件名由旧的 v18.52 修正为 v18.59。
- `/api/state` 仅保留登录/退出/会话用途；旧导航状态 GET/PUT 返回 410，避免继续写入旧 `nav_state/nav_backup`。旧表暂不删除，以便 `/api/data` 仍可完成一次性迁移。
- 将末尾 15 个连续兼容 CSS 补丁合并为一个 style 标签，保持原 CSS 顺序，不改变视觉级联。
- 删除已无引用的旧 WebStack 静态 assets，降低项目体积。

## 下一阶段建议（V18.60）
1. 浏览器书签 HTML 导入：Chrome/Edge HTML -> 分类预览 -> 重复检测 -> 写入 D1。
2. 完整灾备包：D1 导航数据 + 分类/排序/收藏/删除状态 + KV 自定义图标清单。
3. 网站状态检测二级策略：HEAD 不确定时用轻量 GET/Range 复检，连续失败再标记。

## 注意
- 本版本没有删除 D1 中旧 `nav_state/nav_backup` 表；只停止旧接口继续写入。确认长期稳定后，可在管理维护功能中增加“删除旧迁移表”的显式按钮。
